<GradientTitle text="React 技术栈" />

## 1. React基础

### 1.1 React简介

React是Facebook推出的一个用于构建用户界面的JavaScript库。

React的核心思想是：

* 声明式编程：React的核心思想是将UI抽象为一个数据结构，然后通过视图层的声明式来操作数据结构。
* 组件化：将一个复杂的页面拆分成一个个独立的组件，然后通过组件的组合来构建页面。